{
  "name": "assurtrans-workspace",
  "private": true,
  "version": "0.0.0",
  "description": "Espace de travail Assur'Trans : front autonome (web/) et API de reglement (server/).",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "preview": "npm --prefix web run preview",
    "test": "npm --prefix server test && npm --prefix web test",
    "typecheck": "npm --prefix server run typecheck && npm --prefix web run typecheck",
    "scan:secrets": "npm --prefix server run scan:secrets",
    "test:integration": "npm --prefix server run test:integration"
  }
}
